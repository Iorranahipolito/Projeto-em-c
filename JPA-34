#include <stdio.h>
#include <stdlib.h>
#include <locale.h>

int main()
{
setlocale(LC_ALL, "portuguese");
printf("\n**********************************************************************************************************");
printf("\n*Aluno:IORRANA DE SOUZA HIPÓLITO- RA: 0025980                                                            *");
printf("\n*Programa FBR-34 - Número primo                                                                          *");
printf("\n**********************************************************************************************************\n");

int n, i, primo = 1;
printf("Digite um numero: ");
scanf("%d", &n);

if(n <= 1)
    primo = 0;

for(i = 2; i < n; i++) {
    if(n % i == 0) {
        primo = 0;
        break;
    }
}

if(primo)
    printf("Primo\n");
else
    printf("Nao primo\n");


return 0;
}
